# Dependencias para uso do app pelo professor (instalador de duplo clique).
# Espelha o pyproject.toml: nucleo + interface + provedores de LLM.
# O pyproject continua sendo a fonte canonica para desenvolvimento (pip install -e ".[dev]").

sympy>=1.12
pydantic>=2.5

# Interface: servidor HTTP que serve a pagina web. Sem Node, sem passo de build.
fastapi>=0.110
uvicorn[standard]>=0.27

# Exportacao de listas em Word. As duas ultimas convertem a formula em equacao
# nativa do Word (LaTeX -> MathML -> OMML); sem elas a formula sai como texto.
python-docx>=1.1
latex2mathml>=3.77
mathml2omml>=0.0.2

# Provedores de LLM (os tres vao juntos para que a escolha nas configuracoes
# funcione sem reinstalar nada).
anthropic>=0.40
openai>=1.40
httpx>=0.27
